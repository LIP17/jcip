package chapter5.synchronizer;

/**
 * Synchronizer is any object that coordinates the control flow of threads based on its state.
 *
 * Blocking queue, semaphore, latch and barrier all can act as barrier.
 */
public interface Synchronizer {
}
